#include <iostream>
#include <string>
#include <vector>

std::vector<std::string> log;

int main()
{
     while(s = std::cin.getline() && (< !s.empty())
     {
          switch (expression)
          {
          case /* constant-expression */:
               /* code */
               break;
          
          default:
               break;
          }
     }             
}